@inject NavigationManager Navigation
@code {
    protected override void OnInitialized()
    {
        // I'd go /login, but the spec seems to be go to home
        Navigation.NavigateTo($"");
    }
}